/**
 * * Math.random() will always return value between 0.1 - 0.9
 */

// Testing Math.random
const array = Array(100).fill("dummy");
array.map((item, i) => console.log(`Item ${i}`, Math.random()));
// console.log("array", array.length);
// console.log("tt", tt);
